apiVersion: apps/v1
kind: Deployment
metadata:
  name: msmanager
  labels:
    app: msmanager
spec:
  replicas: 3
  selector:
    matchLabels:
      app: msmanager
  template:
    metadata:
      labels:
        app: msmanager
    spec:
      containers:
        - name: msmanager
          image: robertpolanski/msmanager:0.1
          ports:
            - containerPort: 8080
          env:
            - name: CALCULATOR_URL
              value: "http://calculator:8081"